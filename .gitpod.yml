# Commands to start on workspace startup
image:
  file: .gitpod.Dockerfile
tasks:
  - init: yarn
    command: yarn start
# Ports to expose on workspace startup
ports:
  - port: 8000
    onOpen: open-preview